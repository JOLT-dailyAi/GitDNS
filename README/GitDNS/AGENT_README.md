GitDNS Navigation Index

This folder contains navigation indexes for GitHub repositories.
Each subfolder maps one repository and contains two files.

FILES

{owner}_{repo}.zone - primary navigation file for AI agents
  Read the ORIGIN line to get the base raw URL.
  Resolve any file: ORIGIN + relative/path = full raw URL.
  Fetch only files your task requires.

{owner}_{repo}.tree - human-readable orientation only
  Use to identify relevant dirs, then resolve via .zone file.
  Do not ingest this file entirely.

NAVIGATION PROTOCOL
  1. Read .zone - note the ORIGIN base URL
  2. Scan paths - identify files relevant to your task
  3. Fetch on demand - ORIGIN + path = full raw URL
  4. Do not re-read zone repeatedly - navigate from memory

Generated by GitDNS https://github.com/JOLT-dailyAi/GitDNS
